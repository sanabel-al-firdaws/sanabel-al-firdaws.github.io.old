---
interface Props {
	id: String;
}

const { id } = Astro.props;

if (!id) {
	throw new Error("Missing prop `id` on `<hadith>` component.");
}

let Response = await fetch(`http://localhost:5000/v1/site/hadith/${id}`);
let data = await Response.json();



import Card  from './starlight/Card.astro'
---

<span class="hadith">{data.data.hadith}</span>
<!-- 


		<Card title=" ">  
			<span class="hadith">{data.data.hadith}</span>
		<Card title='الراوي:' >
			
			{data.data.rawi}
		</Card>
		<Card title='المحدث:'>
			
			{data.data.mohdith}
		</Card>

		<Card title='المصدر:' >
		
			{data.data.book}
		</Card>
		<Card title='الصفحة أو الرقم :'>
			
			{data.data.numberOrPage}
		</Card>

		<Card title='خلاصة حكم المحدث :'>
			
			{data.data.grade}
		</Card>
		</Card>
	 -->


<style is:inline>

	.hadith {
		color: var(--sl-color-gray-1);
	
	}



</style>
