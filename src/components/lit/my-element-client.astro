---
import { SimpleGreeting } from './my-element';

interface Props {
	name: String;
}

let { name } = Astro.props;

if (!name) {
	throw new Error('Missing prop `name` on `<SimpleGreeting>` component.');
}

---

<SimpleGreeting name={name} />
    
